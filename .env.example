# Required: Anthropic API key for product discovery + landing page generation
ANTHROPIC_API_KEY=

# Required: Vercel KV for storing generated products (auto-set by Vercel KV integration)
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=
KV_URL=

# Required: secret to protect the cron + generate endpoints
CRON_SECRET=

# Optional public URL of the site (for OG metadata)
NEXT_PUBLIC_SITE_URL=
